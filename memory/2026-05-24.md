## 2026-05-24 — PMSC API SaaS + Personal/Prod Split

### Özet (bu session)
- PMSC API’yi RAG-as-a-Service yönünde multi-tenant hale getirip prod smoke test yaptık.
- Prod ve personal instance’ları ayrıldı: OpenClaw artık **personal** Railway service’ine yazıyor; customer prod datası ayrı kaldı.

### Versiyonlar / Release’ler
- v0.3.0: Multi-tenant SaaS altyapısı (tenant isolation, usage, admin key endpoints, rate limit temeli)
- v0.3.1: Mevcut SQLite DB’lerde `tenant_id` kolonu yoksa otomatik migration (ALTER TABLE) → prod’da 500 fix.
- v0.3.2: Prod contract alignment:
  - Admin header ayrımı: admin endpoint’leri `X-Admin-Key` (tenant endpoint’leri `X-API-Key`).
  - `/admin/keys/create` request body ile opsiyonel `tenant_id`, `tier`, `label`; response `api_key` + onboarding info.
  - `/store/add` lite mode: payload/index opsiyonel; `text + vector_json` ile kayıt.
  - Swagger UI `/docs` ve `/redoc` aktif.
  - OpenAPI spec `openapi.json` repo’ya eklendi.

### Prod smoke test sonuçları (pmsc-api-production)
- `/health` OK (0.3.2)
- Tenant izolasyon OK (farklı tenant’lar birbirini görmüyor)
- `/usage` OK
- Free tier rate limit: 101. istekte 429 (limit 100/day)

### Personal vs Prod ayrımı
- Personal URL: https://pmsc-api-personal-production.up.railway.app
- Prod URL: https://pmsc-api-production.up.railway.app
- Personal ve prod birbirinden bağımsız; aynı repo üzerinden iki Railway service.
- Port mismatch kaynaklı personal 502 sorunu: domain portu 8080’e alınarak çözüldü.
- Son izolasyon kontrolü: prod’da yeni tenant collections boş; personal’daki marker koleksiyonu prod’da görünmüyor.

### Güvenlik notu
- Admin key / API key gibi secret’lar dosyaya yazılmadı.

### Süre / ölçüm
- Bu chat session süresi: ~1 saat 25 dakika (OpenClaw session_status).
