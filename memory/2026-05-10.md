## 2026-05-10

- Generated SSH key for agent access on claw:
  - private: `~/.ssh/openclaw_ed25519`
  - public:  `~/.ssh/openclaw_ed25519.pub`
  - Purpose: allow Elif/OpenClaw to SSH to hosts where this pubkey is added to `~/.ssh/authorized_keys`.
  - Note: do not delete/rotate without updating authorized_keys on target hosts.
