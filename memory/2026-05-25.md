## 2026-05-25
- Cron job `qwen3-importance-scorer` OpenClaw tool allowlist hatası verdi: runtime `toolsAllow` içinde `functions.exec` var ama kayıtlı/izinli tool yok; bu nedenle job çalışmadı.
- Çözüm: importance scorer’ı OpenClaw iç cron yerine OS cron/systemd timer ile çalıştırmak veya OpenClaw’da `exec` tool’unu/plugin’ini etkinleştirip allowlist’i düzeltmek.
