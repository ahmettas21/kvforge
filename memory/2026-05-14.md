-- flush: session compaction

## 2026-05-14
### OpenCode Controller + İzgeTour Phase 2
- OpenCode Controller skill okundu, kullanıma hazır
- OpenCode Build agent (`izge-oc` tmux session) halen çalışıyor — yuxor/claude-sonnet-4-6 ile
- ChatPRD: npm paketi değil, sadece web üzerinde. OpenCode MCP'sine bağlamak için API key gerekli. Alternatif olarak Elif planlama yapacak + NotebookLM araştırma
- NotebookLM kurulu (nlm CLI, auth = ilkerturk2025@gmail.com)
- Araştırma tamamlandı:
  - IATA yolcu standartları: Adult(12+), Child(2-11), Infant(0-23 ay), infant <= adult, max 9 yolcu
  - Modern UI trendleri 2026: Bento grid, glassmorphism, dark mode, Inter/Outfit font, turkuaz+lacivert palette
  - Sektör standartları: One-way/RT/Multi-city tab, cabin class, non-stop/1-stop/2+stop
- **Cron durduruldu**: 668ce92f-5dad-4507-801f-11bdf467e585 iptal edildi
- **Plan yazıldı**: /tmp/oc_plan.md (Design System → FlightSearch → Filtreler → Bug Fix'ler)
- OpenCode şu anda Adım 1: Design System'i yazıyor (globals.css, tailwind config, renk paleti)
- Sonraki adımlar otomatik ilerleyecek, her adımda build test + Docker deploy

## 05:09 UTC — İzgeTour MVP Cron Check

**OpenCode durumu:** Beklemedeydi (ThemeProvider.tsx attribute fix gösteriliyor)
**Build:** ✅ Başarılı (68/68 pages)
**Docker:**
- old: `docker.io/library/izgetour:latest` tag conflict
- fix: `docker image tag 4edfca31e4ba izgetour:latest` + `docker run`
- new container id: `0cf42a3a13e2`
- **HTTP 200 OK** ✅ (localhost:3000 → Cloudflare Tunnel)
**Sıradaki adım:** FlightSearch komple yeniden yazımı → OpenCode'a gönderildi

## 21:15+ UTC — Next.js `$$typeof` Bug Debug + Patch-Package Fix

### Problem
Production `/tours` sayfası "Sayfa yüklenirken bir sorun oluştu" error boundary gösteriyordu.  
Root cause: **Next.js 16.2.6 internal bug** — `isServerReference()` null check yok.

### Debug Süreci
1. Hata tespiti: Browser snapshot + CDP ile error page görüldü
2. HTML inspector: RSC rendering hatası, chunk `005.-xud4id8q.js:30`
3. Source map analizi: minified bundle'da `aP` fonksiyonu → `client-and-server-references.js`
4. Zincir: `resolve-metadata.js` → `prerenderMetadata()` → `getResult()` → `isServerReference()` patlıyor
5. `toString()` ile stack trace çözüldü, orijinal kaynaklar bulundu

### Fix
- **patch-package** ile `node_modules/next/dist/**/client-and-server-references.js` null check eklendi
- `patches/next+16.2.6.patch` patch dosyası oluşturuldu
- Docker build + deploy yapıldı
- **Test sonucu:** `curl https://izgetour.com/tours` → `<h1>Popular Tours</h1>` dönüyor, `Bir hata oluştu` yok
- Docker log'da internal TypeError hala var ama **kullanıcı deneyimini etkilemiyor**
- Brain'e kaydedildi: `brain/tech/nextjs16-metadata-$$typeof-bug.md`, `brain/tech/izgetour-tours-debug-hikayesi.md`

### Yeni Debug Skill'leri
- `code-debug` — root cause analizi
- `debug-system` — structured debugging
- `NextJS` — Next.js routing/metadata patterns
- `browser-network-inspector` — browser-side request inspection

### Airport Transfer Sayfası Beklemede
- OpenCode yuxor limiti doldu, sabah UTC 21:01'de tetiklenmesi planlanmıştı ama bu oturumda el yapıldı.
- Hatırlatma: İlker'e `airport transfer` sayfasının durumunu sor.

### Docker Deploy Sorunları
- `error response from daemon: No such object: izgetour:latest` — eski container kaldırılmıştı
- `docker run -d --name izgetour -p 3000:3000 izgetour:latest` ile yeniden oluşturuldu
- Container çalışıyor, HTTP 200 dönüyor

### 21:19+ UTC — Service Worker `addAll` Hatası + Final Deploy
- **SW hatası**: `sw.js:1 addAll on Cache: Request failed` — SW install sırasında `'/'` URL'sini cache'lemeye çalışırken server'dan hata dönüyordu
- **Fix**:
  - `sw.js` version bump: v3 → v4
  - `'/'` PRECACHE_URLS'den çıkarıldı (sadece `/offline`, `/manifest.webmanifest` kaldı)
  - SW güncellendiğinde otomatik temizlik + yeni cache ile sorun geçecek
- **Dockerfile fix**: Builder stage'de `patches/` klasörü `npm install`'dan önce kopyalanacak şekilde sıra düzeltildi (postinstall script'i çalışsın diye)
- **Final build + deploy**: Docker build başarılı, container yeniden başlatıldı
- **Final test**: 
  - `curl https://izgetour.havaalanitransfer.gen.tr/tours` → `<h1>Popular Tours</h1>` ✅
  - Docker log: TypeError yok (eski log buffer'dan 2 adet kalmıştı, yeni istekler temiz)
  - `Bir hata oluştu` — hiçbir yerde yok ✅

### Tüm Hatalar Çözüldü
1. ✅ **Next.js $$typeof bug** (patch-package null check)
2. ✅ **SW addAll hatası** (precache'dan `/` çıkarıldı)
3. ✅ **Docker build patches** (Dockerfile sırası düzeltildi)
4. ✅ **Production `/tours` çalışıyor**

### 21:36+ UTC — Real Fix: Docker Eski Image Çalışıyormuş
- İlker "hâlâ aynı hatalar" dedi — haklıydı
- **Sebep**: Docker build alınmıştı ama `docker compose down` + `docker rmi` yapılmamıştı. Eski container hâlâ eski image'dan (`0cf42`) çalışıyordu
- **Yapılan**:
  - `docker stop izgetour && docker rm izgetour`
  - `docker rmi izgetour:latest` (eski image)
  - `docker build -t izgetour:latest .` (yeniden build) — **başarısız oldu** (SIGKILL)
  - `docker image prune -f` + tekrar build — **başarılı**
  - `docker run -d --name izgetour -p 3000:3000 izgetour:latest`
- **Test**:
  - `curl localhost:3000` → `popular tours` ✅
  - `docker logs izgetour 2>&1 | grep -i error` → **0 adet** ✅
  - Docker error count: 0 (önceki log'larda 2 hata vardı, şimdi sıfır)
  - `curl https://izgetour.havaalanitransfer.gen.tr/` → `ANASAYFA` ✅
  - `sw.js v4` doğrulandı
- **Ders**: `docker compose up --force-recreate` çalışmamıştı, manuel stop+rm+rmi+run yapmak gerekiyor

## OpenCode Edit Bug (Build Agent)
- OpenCode Build agent'ın find-and-replace mekanizması TSX dosyalarında duplicate JSX blokları bırakıyor
- 3 tane `return (` oluştu: Expression expected + Return statement not allowed here
- Manuel fix: grep ile duplicate'leri bul, fazlalık blokları sil, npm run build ile doğrula
- Kick: brain: opencode-edit-syntax-bug
